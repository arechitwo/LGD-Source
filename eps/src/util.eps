/*
 * util.eps
 * Purpose: Utility helpers for hyper triggers, stacking, and HUD.
 * Responsibilities: Provide common trigger snippets used across modules.
 * Dependencies: constants.eps, state.eps
 */

function HyperTriggersAll() {
    // TODO: enable hyper triggers for all players
}

function StackAddDC(p, unitID, n, inc, dcUnit) {
    // TODO: loop over unitID count and add n then inc per unit to dcUnit
}

function StackAddTerritory(p, unitID, n) {
    // TODO: increment territory DC based on owned unit count
}

function HudEvery3s(p) {
    // TODO: display gold/food/faith to player p every 3 seconds
}

/* HOW TO EXTEND
 * - Add debug print utilities or logging helpers.
 * - Implement advanced HUD elements like upkeep or cooldown timers.
 */
