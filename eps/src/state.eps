/*
 * state.eps
 * Purpose: Macros and global state helpers.
 * Responsibilities: Provide DC manipulation macros for modules.
 * Dependencies: constants.eps
 */

// Death counter manipulation macros
macro SET_DC(p, dc, value) {
    // TODO: set death counter dc for player p to value
}

macro ADD_DC(p, dc, value) {
    // TODO: add value to death counter dc for player p
}

macro SUB_DC(p, dc, value) {
    // TODO: subtract value from death counter dc for player p
}

/* HOW TO EXTEND
 * - Implement GET_DC macro for reading values.
 * - Add persistent state structures or arrays as needed.
 */
